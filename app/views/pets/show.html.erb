<div>
  <div class="pet-details">
    <h2><%= @pet.name %></h2>
    <p><%= @pet.address %></p>
    <p><%= @pet.details %></p>
  </div>

  <p><%= %></p>

  <p>test</p>

  <%= cl_image_tag @pet.photo.key, height: 300, width: 400, crop: :fill %>


  <%= link_to "Hangout with this pet (bookings work in progress)",
    pet_path(@pet)
  %>



  <%= link_to "Hangout with this pet (bookings work in progress)",
    new_pet_booking_path(@pet)
  %>

  <%= link_to "Edit this pet",
    edit_pet_path(@pet)
  %>

  <%= link_to "Delete",
    pet_path(@pet),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %>

  <%= link_to "Back to home", pets_path(@pet)%>
</div>
