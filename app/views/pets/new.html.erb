<h1>create new pet</h1>
<%= simple_form_for @pet do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :details %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to list", pets_path(@pet)%>
