<h2>Updating a pet</h2>

<%= simple_form_for(@pet) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :details %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to list", pets_path(@pet)%>
